apiVersion: apps/v1
kind: Deployment
metadata:
  name: shippingservice
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "ms-k8s-platform.shippingServiceLabels" . | nindent 4 }}
spec:
  replicas: {{ .Values.shippingService.replicas }}
  selector:
    matchLabels:
      {{- include "ms-k8s-platform.shippingServiceSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "ms-k8s-platform.shippingServiceSelectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: shippingservice
          image: "{{ .Values.shippingService.image.repository }}:{{ .Values.shippingService.image.tag }}"
          imagePullPolicy: {{ .Values.shippingService.image.pullPolicy }}
          ports:
            - name: grpc
              containerPort: 50051
          env:
            - name: PORT
              value: "50051"
            - name: DISABLE_PROFILER
              value: "1"
            - name: DISABLE_TRACING
              value: "1"
            - name: DISABLE_STATS
              value: "1"
          readinessProbe:
            periodSeconds: 5
            grpc:
              port: 50051
          livenessProbe:
            grpc:
              port: 50051
          resources:
            requests:
              cpu: {{ .Values.shippingService.resources.requests.cpu }}
              memory: {{ .Values.shippingService.resources.requests.memory }}
            limits:
              cpu: {{ .Values.shippingService.resources.limits.cpu }}
              memory: {{ .Values.shippingService.resources.limits.memory }}
---
apiVersion: v1
kind: Service
metadata:
  name: shippingservice
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "ms-k8s-platform.shippingServiceLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "ms-k8s-platform.shippingServiceSelectorLabels" . | nindent 4 }}
  ports:
    - name: grpc
      port: 50051
      targetPort: 50051
