apiVersion: apps/v1
kind: Deployment
metadata:
  name: recommendationservice
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "ms-k8s-platform.recommendationServiceLabels" . | nindent 4 }}
spec:
  replicas: {{ .Values.recommendationService.replicas }}
  selector:
    matchLabels:
      {{- include "ms-k8s-platform.recommendationServiceSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "ms-k8s-platform.recommendationServiceSelectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: recommendationservice
          image: "{{ .Values.recommendationService.image.repository }}:{{ .Values.recommendationService.image.tag }}"
          imagePullPolicy: {{ .Values.recommendationService.image.pullPolicy }}
          ports:
            - name: grpc
              containerPort: 8080
          readinessProbe:
            periodSeconds: 5
            grpc:
              port: 8080
          livenessProbe:
            periodSeconds: 5
            grpc:
              port: 8080
          env:
            - name: PORT
              value: "8080"
            - name: PRODUCT_CATALOG_SERVICE_ADDR
              value: "productcatalogservice:3550"
            - name: DISABLE_PROFILER
              value: "1"
          resources:
            requests:
              cpu: {{ .Values.recommendationService.resources.requests.cpu }}
              memory: {{ .Values.recommendationService.resources.requests.memory }}
            limits:
              cpu: {{ .Values.recommendationService.resources.limits.cpu }}
              memory: {{ .Values.recommendationService.resources.limits.memory }}
---
apiVersion: v1
kind: Service
metadata:
  name: recommendationservice
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "ms-k8s-platform.recommendationServiceLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "ms-k8s-platform.recommendationServiceSelectorLabels" . | nindent 4 }}
  ports:
    - name: grpc
      port: 8080
      targetPort: 8080
