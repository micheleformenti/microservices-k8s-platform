{{- if and .Values.frontend.gateway.enabled .Values.frontend.gateway.tls.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: frontend-https
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "ms-k8s-platform.frontendLabels" . | nindent 4 }}
spec:
  parentRefs:
    - name: frontend
      sectionName: https
  {{- with .Values.frontend.gateway.hostname }}
  hostnames:
    - {{ . | quote }}
  {{- end }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: frontend
          port: 80
{{- end }}
